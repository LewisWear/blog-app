export default class {
    constructor(name, blogTitle, done, deleted) {
      this.name = name;
      this.blogTitle = blogTitle;
      this.done = done;
      this.deleted = deleted;
      this.date = new Date();
    }
  }
  